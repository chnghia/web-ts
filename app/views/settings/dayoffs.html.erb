<div class="block" id="block-text">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><a href="/settings">Settings</a></li>
      <li><a href="/settings/departments">Departments</a></li>
      <li><a href="/settings/timeshifts">Timeshifts</a></li>
      <li class="active"><a href="/settings/dayoffs">Dayoffs</a></li>
      <li><a href="/settings/messages">Messages</a></li>
    </ul>
  </div>
  <div class="content">
    <div class="control">
      <a href="/dayoffs/new" class="button" title="New dayoff">
        <img alt="New department" src="/images/web-app-theme/icons/clock_add.png"> New
      </a>
    </div>
    <h2 class="title">Dayoffs</h2>
    <div class="inner">
      <table class="table">
        <tr>
          <th class="first">ID</th>
          <th>Name</th>
          <th>Type</th>
          <th>Occur</th>
          <th>Date</th>
          <th><%= t("web-app-theme.created_at", :default => "Created at")  %></th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @dayoffs.each do |dayoff| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= dayoff._id %>
          </td>
          <td>
            <%= dayoff.name %>
          </td>
          <td>
            <%= dayoff.type %>
          </td>
          <td>
            <%= dayoff.occur %>
          </td>
          <td>
            <%= dayoff.date_s %>
          </td>
          <td>
            <%= dayoff.created_at %>
          </td>
          <td class="last">
            <%= link_to "#{t("web-app-theme.show", :default => "Show")}", dayoff_path(dayoff) %> |
            <%= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_dayoff_path(dayoff) %> |
            <%= link_to "#{t("web-app-theme.delete", :default => "Delete")}", dayoff_path(dayoff), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}" %>
          </td>
        </tr>
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
        </div>
        
        <%= paginate @dayoffs  %>
        
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>
